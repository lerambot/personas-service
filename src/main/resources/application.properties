spring.application.name=personas-service

# Configuración de puerto
server.port=8080

# Configuración de base de datos (ajusta estos valores según tu entorno)
spring.datasource.url=jdbc:postgresql://localhost:5432/seguros_db
spring.datasource.username=postgres
spring.datasource.password=Postgres2025
spring.datasource.driver-class-name=org.postgresql.Driver


# Configuración de JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=miClaveUltraSeguraParaJwtConMinimo32Caracteres

